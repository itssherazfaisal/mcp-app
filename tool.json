[
    {
      "name": "generate_vehicle_report",
      "description": "Generate a vehicle health check report from video + JSON",
      "parameters": {
        "type": "object",
        "properties": {
          "video_url": {
            "type": "string",
            "description": "S3 URL to the MP4 video"
          },
          "vhc_json": {
            "type": "object",
            "description": "JSON report from the vehicle health check"
          },
          "debug": {
            "type": "boolean",
            "description": "Enable debug mode for extra logs"
          }
        },
        "required": ["video_url", "vhc_json"]
      }
    }
  ]
  